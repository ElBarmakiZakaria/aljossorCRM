things to done
    dowload exel file
    \\delete in payment tables
    if a student only stoped one subject figure how to work with active statue and so on
    if you want to delete some one from one table